{
  "name": "@ielts-map/core",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic domain layer: types, dedup, geo confidence, filtering. Shared by web and (future) React Native clients.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./dataset": "./src/dataset.ts"
  },
  "files": ["src", "data"],
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
